# backend/ — Litestar API + worker (Python)

Self-hosted backend: Litestar + SQLAlchemy 2.0 async + Alembic + SAQ, deployed on a
single EC2 box via docker-compose.

Dev commands live in the root `Justfile`
(`just install`, `just dev-backend`, `just db-start`, `just test`).
